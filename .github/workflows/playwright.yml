name: Playwright CI

on:
  workflow_call:

jobs:
  playwright-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Cache Node Modules
        uses: actions/cache@v4
        with:
          path: Hunt_Career_Playwright/node_modules
          key: ${{ runner.os }}-node-playwright-${{ hashFiles('Hunt_Career_Playwright/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-playwright-

      - name: Install Dependencies
        run: npm install
        working-directory: Hunt_Career_Playwright

      - name: Run Playwright Tests
        run: npx playwright test --reporter=html
        working-directory: Hunt_Career_Playwright

      - name: Upload Playwright Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: Hunt_Career_Playwright/playwright-report
